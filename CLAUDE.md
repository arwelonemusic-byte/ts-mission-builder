# TS Mission Builder

Web-based mission creation tool for Arma Reforger. Community members design a mission in the browser (map, factions, AI zones, briefing) and the tool generates a complete, workshop-ready mission addon as plain text files. Workbench is needed only to open the project once (regenerates `resourceDatabase.rdb` / missing `.meta`) and to publish.

Feasibility research + decisions (2026-07-04): see the report in Obsidian — `C:\Users\djdav\Obsidian Vaults\Alex's Vault\Inbox\Web-based mission making — Feasibility Report.md` (brief: `Web-based mission making.md`, foundation spec: `Mod Defaults.md`).

## Division of labor

- **TS Mission Toolkit** (`C:\Users\djdav\Documents\My Games\ArmaReforgerWorkbench\addons\TS Mission Toolkit`) = runtime: prefabs, ScenarioFramework plugins, loadout/arsenal/fire-support systems. Generated missions depend on it (`.gproj` dependency GUID `6906F4528B72651A`).
- **TS Mission Builder** (this repo) = authoring: web app + generator library that emits mission addons. Never put builder files in the toolkit repo and never put addon resources here.
- Generated output goes to `C:\Users\djdav\Documents\My Games\ArmaReforgerWorkbench\addons\<AddonName>` (current spike target: `TS_WebSpike`).

## Repo layout

npm workspaces: `generator` (package `mission-gen`) + `web`. Root `npm install`; dev server: `npm run dev` in `web/` (localhost:3000); typecheck/build: `npm run build` in `web/`.

- `generator/lib.mjs` — pure generator library (`buildMissionFiles(mission)` → `{files, guids, addonDirName}`), runs in Node AND the browser. `generator/catalogue.mjs` — the GUID catalogue (terrains, factions, `ZONE_MODULES` UI list) + the `MODS` registry merge. `generator/mods/*.mjs` — per-mod content registries (currently `rhs.mjs`). `generator/generate.mjs` — CLI wrapper that builds the TS_WebSpike test mission (`node generate.mjs`; `--rhs` builds the TS_WebSpikeRHS variant exercising the mod path).
- `web/` — Next.js 16 + Tailwind v4, styled to match ts-ops-planner (full-bleed map, floating 360px panel, yellow `#f4db50` accent). `src/app/page.tsx` = the editor shell (step tabs Mission/Factions/Spawn/Zones/Markers/Brief + GENERATE, all state lifted here, localStorage key `ts-mission-builder-v1`); per-step panels in `src/components/panels/`; `src/components/MissionMap.tsx` = lean imperative Leaflet map (CRS.Simple with flipped-Y transformation, XYZ tile pyramids from `public/tiles/` — tiles required, no single-image fallback; lat=worldZ lng=worldX, 1 unit = 1 m); `src/lib/export.ts` = mission state → generator input (heightmap Y sampling) → File System Access API folder write (Chrome/Edge only); `src/lib/heightmap.ts` = bilinear sampler from ts-ops-planner **with a grid-registration fix (2026-08-04, engine-validated)**: pixel i sits at exactly i·cellSizeM (the extractor keeps every step-th terrain vertex), so px-per-metre = 1/cellSizeM — the original `(widthPx-1)/worldWidthM` mapping stretched the grid ~0.15% (Arland: last vertex 4090 m vs worldWidth 4096) and read terrain ~3 m south-west of the true point at mid-map, sinking slope-placed props ~1 m; after the fix the sampler matches Workbench `getHeight` to the cm on grid vertices (ts-ops-planner still carries the stretched mapping — port the fix if its heightmap use ever becomes precision-sensitive); **mission file (.json)** = the browser-agnostic escape hatch for the Chrome/Edge-only addon export — `downloadMissionJson`/`parseMissionJson` in `mission.ts` (Mission tab → Save/Load to file, plain Blob + `<a download>`, no File System Access API) let a Firefox user hand their mission to someone who can generate it. Both localStorage loads and file imports run the SAME `migrate()` in `mission.ts` — keep it additive and never let it throw or bail to `newMission()`: `page.tsx` writes the loaded mission straight back to localStorage, so a bail overwrites the user's save; `src/lib/terrains.ts` = 3 vanilla terrains (tile pyramids + heightmaps copied from ts-ops-planner).

## 3D view (2026-07-31)

`web/src/components/MissionMap3D.tsx` (~1000 lines, raw three.js ^0.184 + CSS2DRenderer, ported from ts-ops-planner's replay-only `MapClient3D` and made editable). Toggle = "3D" button in the shared top-right HUD cluster (`MapViewControls.tsx`, used by both views), desktop-only. Core editing works in 3D: place/select/drag spawn bundle, zones, markers, QRF origins (multi-drop, live dashed line + distance pill); sectors render projected with a center-chip for select/move — **sector draw/resize/rotate stay 2D-only** (`armSectorDraw` auto-switches to 2D). Escape/placeMode/banner/panel flows are page-side and work unchanged.

- **Swap strategy (page.tsx)**: Leaflet stays mounted inside a `hidden` wrapper (its ResizeObserver → invalidateSize handles re-show) so 2D pan/zoom survives; 3D mounts on demand. Per-view API refs `mapApi2d`/`mapApi3d` + `mapApi()` accessor reading `view3DRef` — a single ref goes stale across toggles. Entering 3D captures the 2D viewport via `MapApi.getView()` → `initialView` prop.
- **Terrain** = one BufferGeometry, `seg = min(heightmapPx-1, 256)`, vertices from the shared sampler cache (`lib/terrainSampler.ts`, extracted from export.ts). Texture = `lib/tileComposite.ts`: the existing XYZ tile pyramid stitched into a ≤6144px canvas at runtime (deepest z that fits; missing edge tiles keep the `#0d0f11` fill; LRU cache of 3; key off `TerrainConfig` fields only — eden→everon/cain→kolguyev folder mismatch). No stitched JPEGs shipped.
- **Draped overlays hug the RENDERED mesh, not the raw heightmap**: `meshY()` interpolates the same decimated grid `buildTerrain` filled (triangle-exact against its diagonal split) + lift (lines 1.5 m, fills 0.75 m + polygonOffset, icons 1.5 m). `pickTerrain()` = analytic ray-march over that grid (bisect on sign change) — cheap enough for pointermove picking; used for everything (clicks, drags, MMB pivot, wheel anchor, coords readout). `Raycaster.intersectObjects` only for the few spawn-bundle pick quads.
- **Icons/handles are CSS2DObjects reusing the exact 2D divIcon HTML** — builders extracted to `lib/overlayHtml.ts` (markerHtml/zoneDotHtml/originBadgeHtml/distancePillHtml/sectorChipHtml/pingHtml; MissionMap wraps them in L.divIcon). Screen-constant for free; interactive ones set `pointerEvents:auto` (CSS2D layer is `none`) so their pointer events never reach the canvas — camera pan suppressed structurally. Shared drag controller: 4 px click-vs-drag threshold, pointer capture, live redrape via in-place position-buffer refills, commit on pointerup (React state → full `syncOverlays()` clear-and-rebuild, same dep list as the 2D redraw effect).
- **Canvas clicks**: LMB up with < 5 px movement → `pickTerrain` → `onMapClick` — feeds the whole page-side placeMode dispatcher with zero page changes (no suppressClickRef needed in 3D).
- **Gotchas fixed the hard way (don't regress)**: (1) the hidden 2D map must SKIP the focus effect and return null from `getView` when its size is 0 — fitBounds on a display:none map lands on a garbage max-zoom view that then poisons the 3D handoff; (2) after the async sampler resolves, lift the camera rig (target+eye) by the terrain height at the target — the initial framing assumes y=0 and a close-in view over high ground otherwise starts INSIDE the terrain (black screen); (3) `initialView` is consumed once via a ref and bounds-checked — a terrain switch while in 3D re-runs setup with coords from the previous world.
- v2 candidates: sector draw/resize/rotate in 3D, AO outside-darkening (2D donut doesn't port to a draped mesh — outline only), zone hover tooltips, 3D camera pose persistence across toggles, touch controls.

## Generation telemetry (2026-07-31)

`web/src/app/api/generated/route.ts`: after a successful addon export, `doDownload` fire-and-forgets the full mission `.json` (incl. the thumbnail data URL — archives are self-contained, re-importable via Load-from-file) to `POST /api/generated`. The route writes `<repo>/.mission-data/missions/<utc>_<slug>.json` + appends a stats line to `.mission-data/events.jsonl` (gitignored). Export success never depends on the request; 8 MB body cap. **Prod gotcha**: the systemd unit is `ProtectSystem=strict` — `/opt/ts-web/ts-mission-builder/.mission-data` is explicitly in `ReadWritePaths` (added 2026-07-31; the dir must exist for the mount, don't delete it). Stats: `ssh slotbot-msk 'wc -l < /opt/ts-web/ts-mission-builder/.mission-data/events.jsonl'`.

## Validated architecture facts (don't re-derive)

- **Offline generation works.** Addon = `addon.gproj`, `Missions/*.conf(+.meta)`, `Worlds/*.ent` (3-line `SubScene { Parent "{GUID}worlds/..." }`) `(+.meta)`, `Worlds/*_Layers/*.layer` (text, LF, UTF-8 no BOM). `resourceDatabase.rdb` is binary — never write it; Workbench recreates it on open/close.
- **Self-assigned GUIDs are accepted.** Random/timestamp 16-hex in hand-written `.meta` files works (validated in Workbench + playtest). Keep resource GUIDs stable across generator re-runs.
- **Clean resource override** = same relative path as the original + `.meta` declaring the ORIGINAL resource's GUID. For prefab overrides, mirror Workbench output (see `addons\Operation NewOp` examples): same parent ref AND same root `ID` as the original file, declare only changed components using the base's component-instance GUIDs (quoted: `SCR_Foo "{GUID}"`). Engine layers deltas — the toolkit's other components survive. Four standard overrides per mission: TS_MissionLoadouts.conf `{2B1F00FB4CED5910}` (loadout list — toolkit default is empty, all entries are new members with fresh GUIDs), LoadoutCrates_Conf.et `{897A36FA3D0A19F8}` (faction affiliation only), TS_CustomArsenal.conf `{DDDE97723E57CC62}`, Briefing.conf `{66D418A7AFBF6FEB}` (reuse the toolkit's entry instance GUIDs for the 3 default sections ids 0-2; user-added extra sections get fresh GUIDs, sequential `m_iEntryID` from 3, `m_eJournalEntryType Custom` + editable `m_sCustomEntryName` — Foxhound pattern; multi-line text = quoted lines joined by `\` + newline).
- **Component overrides in layers/prefabs must reuse the base prefab's component-instance GUIDs** — never mint fresh GUIDs for them. Fresh GUIDs only for NEW array members / nested objects.
- **Mod Defaults standard** (per spec): 6 layers (default/AO/Spawn/Markers/QRF/Props; empty `.layer` files are valid; +Objectives.layer since 2026-07-31 — see the Objectives section); FactionManager callsigns/group presets on the playable faction come from `mission.groups` (`{name, size}[]`, 1–8 groups, size 1–9; UI = Squads section in PlayersPanel), default 1'1–1'4 + 1'6 @ 9/9/9/9/3 when absent — squad-name entry [i] MUST positionally reuse `F.squadBase[0..3]` GUIDs (then `squadFifth` at index 4, minted GUIDs beyond), all 4 squadBase overrides are always emitted even with fewer groups (fallback names `1'N` so overflow auto-groups don't pick up vanilla "3"/"4"), quotes in names are escaped to `'`; `m_sCallsignGroupFormat "%3"`, `m_bIsAssignedRandomly 0` on ALL factions' `SCR_FactionCallsignInfo` (default 1 hands out squad names in random order); GameModeSF spawn logic WITHOUT `m_sForcedFaction`; LoadoutManager entries Arsenal/Rifleman (names inherited from `LoadoutManager_Base.et`, set only resource + faction); Spawn layer = faction spawn point + AreaSpawn→LayerCrates/LayerVehicles slots + optional FARP `{0C16FFB1B07F4A89}` (plain placement); MVP plugin set = AIPatrol, MountedPatrol, Fortification, QRFFoot, QRFMounted, SmartGarrison. Every generated Area sets `m_bDynamicDespawn 1` + `m_iDynamicDespawnRange` = zone radius + 600 (was +400 until 2026-08-02).
- All catalogue GUIDs (terrains arland/eden/cain + navmeshes, faction entries, callsign infos, vanilla squad-name members, characters, vehicles, arsenal items) are ground-truthed in `generator/generate.mjs` — extend, don't re-harvest.

## Mod support (RHS, British Forces, Middle East Insurgents, Bandits)

- **Registry**: `generator/mods/rhs.mjs` exports a mod def `{id, label, workshopUrl, dependencies, factions}`; each mod faction has the SAME shape as vanilla `FACTIONS` entries except `confRef` (faction .conf resource ref) replaces `entryGuid`. `catalogue.mjs` merges mod factions into `FACTIONS` with a `mod` tag — resolvers, lib.mjs and all panels work off the one registry. Web types: `web/src/types/mission-gen.d.ts` (hand-written — update it when the registry shape changes); `factionMeta()` in `mission.ts` is the typed view for `mod`/`label`.
- **Faction friendliness is SYMMETRIC and one-sided** (`SCR_Faction.c`: one faction listing the other in `m_aFriendlyFactionsIds` makes BOTH friendly at init) — a declared friendship between a mission's two sides means no combat. Registry defs carry `friendlyWith` (in-game keys: UK→US, RHS_USAF→US, RHS_AFRF→USSR); when either mission side declares the other side friendly (aliases resolve to their base key first), lib.mjs emits an empty `m_aFriendlyFactionsIds {}` in that faction's FactionManager member override, clearing the friendship for that mission only (PLAYTEST-VALIDATED 2026-07-29: RHS_AFRF-vs-MEI-v1-alias shot at each other with the cleared list; the resolved faction showed an empty list in Workbench). With MEI now a real faction, the pairings that still rely on clearing are US-vs-UK, US-vs-RHS_USAF and USSR-vs-RHS_AFRF.
- **Dependencies derive from USAGE, not the UI checkbox**: lib.mjs unions `MODS[id].dependencies` for the mods of the playable + enemy factions only — a vanilla-only mission never lists mod deps even with the mod enabled. `mission.mods` (web state, migration in `loadMission`) only gates which factions the UI offers; Mission-tab checkboxes live in MissionPanel; disabling a mod cascades factions back to US/USSR (`setMods` in page.tsx).
- **FactionManager (validated the hard way)**: RHS OVERRIDES the vanilla `FactionManager_Editor.et` prefab (same path/parent/root ID), appending its factions as members with `m_bIsPlayable 0` — so RHS factions carry `entryGuid` (USAF `{5CC8DE37E1FF0F7A}`, AFRF `{5978B9CE6585BBE8}`, ION `{623962205CE2B89C}`) and mission layers override those EXISTING members exactly like vanilla factions (RHS's own Coop_CombatOps layers are the reference). Appending a fresh conf-ref member instead creates a duplicate FactionKey → deploy menu shows "no available factions" at runtime even though Workbench shows `m_bIsPlayable 1`. The `confRef` append path (CIV_ENTRY pattern, `m_bIsPlayable 0` explicit) remains in lib.mjs ONLY for future mods that don't override the prefab. ALL factions of a used mod are emitted; playable gets the standard callsign/preset block. Factions with empty `riflemen` are ENEMY-ONLY (filtered from the playable dropdown) — RHS ION is one.
- **RHS GUID harvesting (validated 2026-07-11)**: the RHS dump (`reference\RHS`) has NO `.meta` files and prefab root `ID` lines are NOT resource GUIDs (always `0000000000000001`) — folder-scanning is worthless, and reforger-item-database `data/rhs/groups.json` GUIDs are all broken for that reason (`characters.json`/`items.json` are catalog-sourced and good). Authoritative sources: `Configs/Editor/PlaceableEntities/*.conf` (groups, systems/spawn points, fortifications), `Configs/EntityCatalog/*/*.conf` (characters, vehicles, inventory items), RHS's own `Prefabs/MP/Managers/Factions/FactionManager_Editor.et` (faction conf GUIDs). Unrecoverable (excluded): USAF FORECON squad, ION URBAN CloseProtectionTeam, plain ION characters.
- RHS addon GUIDs (all three go into Dependencies): main `595F2BF2F44836FB`, Content Pack 01 `1337C0DE5DABBEEF`, Content Pack 02 `BADC0DEDABBEDA5E` (confirmed from `C:\Users\djdav\Documents\My Games\ArmaReforger\addons` folder names). Faction keys `RHS_USAF` / `RHS_AFRF` / `RHS_ION` (item-db uses `ION` + shared `FFA` pool). USAF+ION share vanilla-identical US callsign member GUIDs; AFRF has its own inline callsign object.
- Full raw harvest (superset of rhs.mjs: excluded groups, skipped vehicle variants, large base compositions, arsenal counts, unrecoverables) + provenance: `input/rhs-harvest.md`.
- **Middle East Insurgents (`generator/mods/mei.mjs`, v2 2026-07-29)**: REAL faction from the "MiddleEastInsurgents" mod — key `MEI`, INDFOR-based, entryGuid `{64E5479B63D9AEFF}` (the mod overrides the vanilla FactionManager, same root ID, standard RHS pattern; it also appends a MEC militia member `{64E51887ABD12B07}` we leave untouched — MEC.conf's friendly list referencing unloaded RHS keys logs benign init errors). Dep anchor = "Czech to Arabic" `68DC13E97BF267BD` (voice/cosmetic-only, verified), whose .gproj pulls MiddleEastInsurgents `64CEC8E005828E5D` + RussiantoArabic `65E0AE1A83DA063A` transitively — plain MiddleEastInsurgents alone leaves insurgents speaking Czech (FIA voices). Enemy-only (`riflemen: {}`), ONE "Insurgents" group set (12 native `Group_MEI_*` catalog groups; EnemyPanel hides the troops list for single-set factions; loadMission drops stale set keys); vehicles/spawnPoint = vanilla FIA refs (the mod points at the vanilla FIA vehicle catalog and ships no spawn points); fortifications = vanilla USSR pools. MEI is hostile to everything but CIV/MEC → works vs USSR AND vs RHS_AFRF with no friendliness clearing. **Mounted-patrol crew rule (found via MEI 2026-07-29, made universal 2026-08-01)**: the MountedPatrol/QRFMounted plugins fill vehicles with the PREFAB's default occupants — vanilla USSR crew for BRDM2/BTR70 and even for UAZ469_PKM_MEI (reskin inherits occupant slots); RHS_AFRF in a vanilla UAZ got vanilla USSR crews the same way (user-reported) → EVERY faction defines `FACTIONS[*].patrolCrew` (~5 concrete character refs) and lib.mjs ALWAYS emits `m_aCrewPrefabPool` (throws on vehicle modules if the pool is missing — new factions can't silently regress). Spikes: `--mei` (US vs MEI), `--afrf-mei` (RHS_AFRF vs MEI). v1 was an `aliasOf: "USSR"` reskin integration via "USSR 2 Middle East" `664AF808E006D8D6` (playtest-validated, then replaced by v2 — real faction beats partial reskin + unlocks USSR-vs-MEI); the alias machinery (catalogue merge, lib.mjs emission filter, page.tsx/EnemyPanel `aliasConflict` guards, `--mei` history) REMAINS in the codebase and is now used by the SFS loadout pack (playable side — see below).
- **`*_Randomized` character prefabs are variant wrappers — broken for INDIVIDUAL spawns only (playtest-refined 2026-07-31, applies to ALL mods)**: they carry `SCR_EditableEntityVariantData` over an often-UNARMED base prefab. Group prefabs whose unit slots reference them resolve FINE (group spawning goes through a variant-resolving path), but individually-spawned characters — `patrolCrew` crew seats (`TS_VehicleCompartmentHelper.FillCompartments`), `hvt`/SlotKill `m_sObjectToSpawn`, any single-character SF slot — spawn the base: unarmed identical clones. Rule: concrete character prefabs for every INDIVIDUAL-spawn field; group refs are unrestricted. Refinement (2026-08-01): `grep -c VariantData` alone over-flags — vanilla `Character_US/USSR_Rifleman` carry a variant table but are FULLY ARMED in their own prefab chain (weapon slots + loadout inline), so they're fine; the broken ones are shells whose own chain is the unarmed base (MEI/Bandit `*_Randomized`). Judge by whether the prefab chain carries weapons, not by VariantData presence. Bonus: a wrapper's variant table lists its concrete variants' GUIDs — that's how the 15 "unrecoverable" plain ION characters were harvested (rhs.mjs patrolCrew + concrete SL hvt).
- **Bandits (`generator/mods/bandits.mjs`, 2026-07-31) — the FactionManager_BASE-append pattern**: "Bandit Faction" `66C4458756B32594` (deps Bandit Gear `66B523AD189F3233` → Zelik's Character `5D0551624969C92E`, all declared → ONE dep GUID, transitive). The mod appends member `{6735141732DAB748}` to vanilla `FactionManager_BASE.et` (same root ID `56B2B4776E6E4499` as Editor — child prefabs keep the root entity ID); vanilla Base declares no members, Editor inherits Base, so the member reaches missions by inheritance and is overridden by instance GUID like Editor-declared ones (Workbench-validated 2026-07-31). Key `PLASTICBANDIT`, enemy-only BY AUTHOR DESIGN ("not set up to be playable" — empty callsign object, no spawn points, all-Randomized characters). No friendly declarations → hostile to every side. One "Bandits" group set (all 7 groups, Squad=10 defense — Randomized slots inside groups resolve fine, see gotcha above); the mod's "UAZ452_Armed_Bandit" is NOT armed (pure budget-cost override, dropped) → patrol pool = vanilla USSR UAZ-469 PKM (captured) + the Bukhanka van (deliberate unarmed troop-carrier option), `patrolCrew` = 6 concrete Scav/Stalker characters; fortifications = vanilla USSR pools; hvt = Stalker "Shadow" (concrete). Spike: `--bandits` (US vs PLASTICBANDIT).
- **US Special Force Squad (Abrashka) (`generator/mods/sfs.mjs`, 2026-08-02) — the playable-only LOADOUT-PACK pattern**: "SFS US Loadout" `69F2D31EB2E09C84` by the user's buddy Abrashka. NOT a faction — 12 modernized SF characters at the vanilla US_Army paths with their OWN resource GUIDs (Workbench-duplicate style, all faction US, concrete/VariantData-free; GUID source = the mod's LoadoutManager_Editor.et override, its only conf — no .meta, no Configs/). Integration = first PLAYABLE use of the `aliasOf` machinery: registry key `SFS_US`, `aliasOf: "US"` (inherits entryGuid/callsigns/spawn point/vehicles/patrolCrew from vanilla US), own label/loadouts ("Special Force Squad" single subfaction)/curated arsenal (Pmag m995, S&W Spec Series, Mk48 AP boxes, M40 M61AP, GM94, SMAW+HEDP, stun grenades, Milsim radio, Vector21, FILBE packs). `playableOnly: true` = new flag: hidden from the enemy dropdown (`enemyFactionKeys` filter in page.tsx + nextEnemy pickers), lib.mjs throws if it ever reaches the enemy side. lib.mjs alias hardening (same commit): `effKey()` moved to top-of-build and applied to crate faction affiliation, LoadoutManager `m_sAffiliatedFaction` ×2 and the `factionEntry` playable comparison — before this, a playable alias serialized its registry key and the US member lost its playable block. Deps: anchor `69F2D31EB2E09C84` alone; RHS arrives transitively via GRS-Weapons (22-addon closure, all junctioned; GRS locker-config log errors are the dep mods' own noise). Full harvest: `input/sfs-harvest.md`. Spike: `--sfs` (SFS_US vs USSR).
- **British Forces (`generator/mods/uk.mjs`, harvested 2026-07-29)**: addon GUID `5AE50EC5B8D6F4AE`, one faction, key `UK`, label "British Military". Same entryGuid pattern as RHS — BF overrides the vanilla FactionManager (same root ID `56B2B4776E6E4499`) appending member `{61500924662B6062}`. Callsign object `{5DA0F2A6677ADA9E}` is inline in UK.conf but copy-pastes vanilla US GUIDs (squad members = vanilla US four, mod renames them A/C/E/G; our override renames again). Subfactions "1989/1983 Regulars" + "1989/1983 Special Forces" (1989 first = default); Reservists exist in the mod but are excluded by design (not playable, no enemy sets). The companion "Truck, Utility" addon `66D74CE7E94C5D05` is deliberately NOT a dependency — as of 1.0.65 it's a pure duplicate of the Land Rover content already inside BF (no Configs/, identical GUIDs; re-check if BF ever dedupes). No `Configs/Editor/PlaceableEntities` groups conf — `Configs/EntityCatalog/UK/UK_Groups.conf` is the group source (the `_1983/_1989` confs only flip `m_bEnabled` per era). Full harvest: `input/uk-harvest.md`. Spikes: `--uk` (UK vs USSR) and `--uk-enemy` (US vs UK).

## Modded maps

Rolled out one at a time (validated in Workbench: Armenhof 2026-07-28, ChernarusMinus 2026-07-29, Faircroft Islands 2026-07-29, Iraq 1990 2026-07-29, Kunar Province 2026-07-29, Ruha 2026-07-29, Serhiivka 2026-07-29, Takistan 2026-07-29, Zargabad 2026-07-29, Zarichne 2026-07-29 — Kunar/Ruha/Serhiivka/Takistan/Zarichne keep their nav refs in a custom SCR_AIWorld_<Map>.et prefab instead of a GM layer; Serhiivka AND Zarichne bake a PerceptionManager into the bare world (`parentHasPerceptionManager: true`); Zargabad's GM soldier slot inherits its navmesh from a Configs/Navmesh/*.conf (follow the conf to the .nmn ref); Zargabad depends on the Takistan ADDON (map-on-map dep, transitive as usual) (grep the whole extraction for NavmeshFile); the .nmn refs slot into our navBlocks the same way. Ruha uses lowercase `worlds/` in resource paths — copy refs verbatim, never normalize case. ALWAYS grep the parent world's default.layer for baked-in managers (PerceptionManager/RadioManager/SCR_AIWorld): Serhiivka's bare world ships a PerceptionManager — set `parentHasPerceptionManager: true` on the TERRAINS entry so lib.mjs skips emitting ours (else "Duplicate entity" error). Serhiivka also ships a terrain-only Serhiivka_Empty.ent — missions parent to the FULL Serhiivka.ent like its own GM world does. Manager Y coords are unreliable ground truth (Faircroft 0, Kunar 10, Serhiivka floating) — cross-check heightmaps against terrain-fitted content entities (buildings) instead). Community maps commonly ship their own log errors (Faircroft: broken pub-sign ref, door navmesh-links; Iraq: bad ProjectileSoundsManager GUID, missing shore config) — judge a generated mission only by errors referencing OUR resources/entities. No UI gating — the terrain dropdown lists every supported terrain (vanilla first, then a disabled-option separator, then modded — `modded`/`workshopUrl` flags in terrains.ts; the Important! callout lists toolkit + map addon + enabled mods dynamically). Per-map recipe:
1. `/extract-addon` the map addon → harvest from the extracted GM world (text): SubScene `Parent` line = the bare-world ref, `*_Layers/default.layer` `NavmeshFile` lines = the 3 nav refs (soldiers/vehicle/lowres — same `SCR_AIWorld` prefab + component-instance GUIDs our generator already emits, only the file refs differ). Skip maps missing any of the 3 navmeshes.
2. `catalogue.mjs` TERRAINS entry: `parent`, `nav[3]`, `dependencies: [<map addon GUID>]` (from the workshop folder name; `58D0FB3206B6F859` = base game, never list it). List ONLY the map addon — its own deps resolve transitively (validated: ChernarusMinus pulls MinusBuildingPack/RailEnfusionRedux(→RailwayGenerator)/OutsideTerrainCore from just the map GUID). lib.mjs unions `TERRAIN.dependencies` into the .gproj deps alongside faction-mod deps.
3. Web: copy `tiles/<key>` + `heightmaps/<key>.bin/.json` from ts-ops-planner (bounds already calibrated there — `worldUR` in its maps.ts = our `worldSize`); add the `terrains.ts` entry. The dropdown picks it up automatically.
4. Junction the map addon AND its whole dep chain into the Workbench addons dir (same as RHS), generate the spike (`node generator/generate.mjs --armenhof` / `--chernarus` pattern), wb_launch + log scan, then user playtest. Large worlds: wb_open_resource may time out client-side while the world still loads fine — poll the console.log for the `Entities load` line instead of retrying.

## Verification workflow

1. `node generator/generate.mjs` (avoid `--clean` while Workbench runs — it deletes the EnfusionMCP handler scripts).
2. Offline check: enfusion-mcp `mod` tool, action `validate`, projectPath = the generated addon.
3. Workbench check: `wb_launch` with the generated `.gproj` → `wb_open_resource Worlds/<name>.ent` → `wb_entity_list`/`wb_layers` → scan the newest `C:\Users\djdav\Documents\My Games\ArmaReforgerWorkbench\logs\logs_*/console.log` + `error.log` for resource errors. Restart Workbench (kill + `wb_launch`) after changing resources on disk — `wb_reload` only reloads scripts.
4. Playtest is manual (user). Dedicated-server testing uses the published Workshop pak, not the local copy.
5. Mod-dependent missions: Workbench does NOT scan the game's workshop dir (`...\My Games\ArmaReforger\addons`) — mod deps resolve via directory junctions into the Workbench addons dir (already created for the 3 RHS packs, e.g. `RHS-StatusQuo_595F2BF2F44836FB`). Without them wb_launch fails with `dependency '...' can't be added`.

## AI group pools

Full vanilla group catalogue lives in `generator/catalogue.mjs` `groupSets` (source: `input/ai-groups.md`), organized per faction → group set (subfaction) → size class. Size rules: **small** (sentry/MG/AT/GL/LAT/suppress/ammo/medical/sapper/engineer/recon/sniper teams) → garrison + patrols; **medium** (fire groups/teams, search groups, platoon HQs) → patrols; **large** (rifle squads, maneuver groups) → patrols + QRF (module not exposed yet). Foot Patrols size classes are per-zone via the patrol-weight slider (2026-07-29): 5 stops = small / small+medium / all / medium+large / large, stored as `ZoneModule.sizes` (absent = module default = all sizes), UI shows only the selected stop's label; lib.mjs uses `p.sizes ?? def.sizes`. Edge cases stay hidden: `resolveGroupPool`'s all-classes fallback silently covers e.g. small-only vs SF sets, and the estimate chip reflects the truly available classes. The Zones panel shows ballpark enemy-count ranges (per zone chip + panel-header total) via `web/src/lib/enemyEstimate.ts`: budget = number of groups (toolkit plugin semantics), class ranges small 2–4 / medium 4–6 / large 6–9, vehicles 3–8 occupants, fortifications 2–4 sentries each, defense group = exact stored size. SF sets (Spetsnaz/Green Berets) reuse their squad as medium+large. Excluded: `*_Base`, `Transport`, `WithDriver`, `LessArmored`. Pool resolution = `resolveGroupPool()` (accepts multiple set keys, merges + dedupes, falls back to all sizes if a class is empty); mounted patrols use `patrolVehicles`. The mission's `enemyGroupSets` array (UI: "Enemy troops" checkbox list, min 1 selected) picks which sets to mix. Full vanilla character list for the future loadout picker: `input/characters-loadouts.md` (not yet in the catalogue).

Defense Group module (kind `slotai`, on/off only): NOT a toolkit plugin — emits a vanilla `SlotAI` child entity inside the zone's Layer (prefab `K.SLOTAI_PREFAB`, component GUID `K.CMP_SF_SLOTAI`, coords 0 0 0 = zone center; entity named `SlotAI<N>` per zone — entity names are world-global). `m_sObjectToSpawn` = the largest squad among selected enemy sets (`groupSets[*].defense` `{ref, size}`, resolved by `resolveDefenseGroup()`, ties → first selected). The slot's default 30 m defend waypoint needs no extra config. NOTE: `ZONE_MODULES[0]` is Defense Group — the fresh-zone default module in page.tsx references AIPatrol explicitly.

QRF Reinforcements modules (2026-07-30, serialization ground truth = Operation Choripan): kinds `qrf-foot`/`qrf-vehicle` → toolkit plugins `TS_ScenarioFrameworkPluginQRFFoot`/`QRFMounted`, UI labels "Foot/Vehicle Reinforcements". User places up to `maxOrigins` (3) "Reinforcement Origins" per module per zone (map click mode like Add zone, per-module buttons that hide at the cap; multi-drop stays armed; origins draggable with live distance pill at the dashed-line midpoint; panel rows ↔ map badges two-way selectable, row click pans/zooms). `ZoneModule.origins` `{x,z}[]` → export samples heightmap Y → lib.mjs emits ONE `$grp TS_QRFSpawnAnchor` in QRF.layer (anonymous instances, `coords` then `m_sAnchorGroup`) + plugin blocks with `m_sAnchorGroup "<ZoneName>_Foot|_Mounted"` (per-zone-per-module — the 5000 m sphere query would cross-match zones otherwise), `m_fTriggerRadius` = zone radius, liberal window `m_fMinSpawnDistance 200`/`m_fMaxSpawnDistance 5000` + `m_fAnchorSearchRadius 5000` (MUST cover the window — default 1500 silently drops far anchors at Init), `m_iAnchorsPerWave` = origin count when >1 (default 1 stacks all groups on the farthest anchor; vehicles at one anchor snap to the SAME road point). Foot pool = large size class; vehicle reuses the patrol-vehicle multiselect + `patrolCrew` crew pool. Zero-origin QRF modules are skipped at emission (plugin would disarm at Init anyway). Estimates: foot = large range ×budget, vehicle = crew range ×budget, counted regardless of origins. Toolkit-side (2026-08-04, playtest-validated): the QRF plugins `PreventMaxLOD()` every spawned agent — the engine deactivates AI at MaxLOD when no player/GM camera is near, which froze far-spawned waves in place (NOT navmesh streaming, no server setting fixes it); dedicated servers get the fix only via the published toolkit pak.

Fortifications module (`TS_ScenarioFrameworkPluginFortification` — singular): composition pools live in `FACTIONS[*].fortifications` `{road, roadside}` (FIA road reuses USSR's `ROAD_FORTS_USSR`); the AI pool is one `groupSets[*].sentry` team per selected enemy set, resolved by `resolveSentryPool()` (suppressed SF sets carry the parent's team, deduped). UI exposes only the budget (`maxBudget: 4` on the module def caps the input). Tuning attrs (roadside offset 15 / min road width 4 / min spacing 50 / single side) equal the plugin's class defaults and are omitted from the serialized block.

## Objectives (in-game tasks, 2026-07-31)

Objectives tab (after Zones — step 06; markers 07, briefing 08) → `Objectives.layer` (7th layer; empty string when no objectives). `mission.objectives` = `MissionObjective { type: "hvt"|"clear"|"reach", x, z, radius?, taskTitle, taskDesc, hintTitle, hintBody }`; add flow = in-panel type picker (`ObjectivesPanel`) → `placeMode "objective"` + `pendingObjectiveType` → one-shot map click with texts prefilled via `t()` in the UI language. Accent `#e8593c` (`OBJECTIVE_COLOR`/`objectiveBadgeHtml` in overlayHtml.ts; 3D `TASK_COLOR` — the pre-existing 3D `OBJECTIVE_COLOR` is the sector rectangle red). Badge draggable + dashed radius circle (clear/reach) in both views. `OBJECTIVE_TYPES` in catalogue.mjs = single source (labels + radius bounds; hvt has no radius).

- **Emission = REAL SF tasks** (decision 2026-07-31; the 12 production ops are all hint-only, but tasks give a task-list entry + auto-completion + JIP visibility): ONE `AreaObjectives` (dynamic despawn OFF — omit the flag, mission-critical entities live from session start) wrapping one LayerTask child per objective. **The LayerTask entity's world name IS the task ID** (`Task_Objective<N>`, world-global unique — `SCR_TaskSystem.CreateTask` rejects duplicates). `m_sFactionKey` = alias-resolved playable key and MUST be a playable faction (empty falls back to "US"; non-playable = task creation fails at Init).
- **`m_eTaskUIVisibility LIST_ONLY`** = task-list entry with NO map marker (objective positions stay hidden; `m_fMarkerUpdateInterval` stays default 0 so nothing tracks a moving HVT). **`m_eTaskNotificationSettings 0`** suppresses the vanilla task popups (default = ALL flags → `PopUpMessage` on created/finished/failed; would duplicate our hint, and popups have the known queue/stacking bugs). Completion hint = fresh-GUID `SCR_ScenarioFrameworkActionShowHint` in `m_aTriggerActionsOnFinish` (`m_iTimeout 8`, `m_iMaxNumberOfActivations 1`, faction-filtered); user newlines serialize as `<br/>`, quotes escape to `'`. **Playtest gate**: if LIST_ONLY leaks position anywhere (esp. on task assignment), fall back to the hint-only production patterns (SlotAI+`SCR_ScenarioFrameworkPluginOnDestroyEvent` / SlotPlayerTrigger+`m_aTriggerActions` — Grasshoppers/Choripan).
- **Per type** (base prefab + component-instance GUIDs in `K.LAYERTASK_*`/`K.SLOT_*`/`K.CMP_*` — reuse, never mint): **hvt** = LayerTaskKill + SlotKill spawning `FACTIONS[enemy].hvt` with `m_bCanBeGarbageCollected 0` (KSC lesson: an unprotected character far from players gets garbage-collected → task uncompletable); completion auto-detected by TaskKill.et's `SCR_TaskKill` damage-state hook; the slot also carries the toolkit's `TS_ScenarioFrameworkPluginHideInBuilding` (baked in, no UI — relocates the officer into the nearest building ≤75 m via sentinel post or navmesh interior query AFTER spawn; the slot stays the spawner so the kill hook is unaffected; no building → stays at the slot). **reach** = LayerTaskMove (component class is plain `SCR_ScenarioFrameworkLayerTask` — the Move shell adds nothing) + SlotMoveTo, plugin sets radius + `m_eActivationPresence PLAYER` (trigger prefab default is ANY_CHARACTER — wandering AI would complete it) + faction. **clear** = LayerTaskClearArea + SlotClearArea with `m_sObjectToSpawn` OVERRIDDEN to `TriggerCharacterSlow`: TriggerDominance is NOT an SF trigger, so `SCR_ScenarioFrameworkPluginTrigger` silently drops every field except radius + owner faction (and its 0.528 ratio would complete with enemies still alive); the SF-trigger route takes `SCR_CustomTriggerConditionsFactionControl` (control ratio ≥ 1, CIV ignored) + `m_fActivationCountdownTimer 5` dwell. **FactionControl gotcha (playtest-caught 2026-07-31)**: the condition counts `trigger.GetEntitiesInside()` — the trigger must track ALL characters, so the plugin block needs `m_eActivationPresence ANY_CHARACTER` EXPLICITLY (plugin default PLAYER is pushed onto the trigger and admits only players to the inside-list) and NO `m_sActivatedByThisFaction` (an owner faction also filters the list); either mistake means enemies are never counted and the task completes the moment a player enters. Net semantics: ≥1 alive character inside AND every non-CIV character inside is playable-faction (ratio is int-division → effectively {0,1}).
- `FACTIONS[*].hvt` = one officer ref per faction (harvest 2026-07-31): US/USSR officers, FIA has no Officer prefab → PL, RHS_USAF = MEF officer (USMC catalog omits it — ref from RHS_Characters_USAF.conf, double-confirmed in an RHS dev world), RHS_AFRF = MSV Flora officer, RHS_ION ships NO officer at all → `Character_RHS_ION_SL_Random`, UK = 1989 Regulars officer, MEI = `Character_MEI_Leader`.
- **deliver** (5th type, 2026-07-31) = "steal/deliver a vehicle": TWO points — objective pos (vehicle spawns from a plain `SlotObjective<N>Vehicle` SlotBase child, GC-protected) and a user-placed delivery point where the LayerTask entity itself sits. Ground truth = vanilla `Compositions/LayerTasks/TaskDeliverVehicles.et`: **NOT LayerTaskDeliver** (that's carryable-intel machinery with possession states) — it reuses LayerTaskMove + SlotMoveTo with `m_eActivationPresence SPECIFIC_PREFAB_NAME` + `SCR_CustomTriggerConditionsSpecificPrefabCount` (`m_bIncludeChildren 1`, count default 1) so the task completes when the chosen vehicle prefab sits inside the delivery trigger (`deliveryRadius` slider 10–100, default 25; nobody needs to be aboard — pushed/AI-driven counts; a same-prefab vehicle from elsewhere in the mission also counts, accepted edge). Web: `objective.delivery {x,z} | null` + `deliveryRadius`; delivery placed/moved via `placeMode "delivery"` + `deliveryTarget`; vehicle picked from `deliverVehiclePool` (ALL enabled factions' vehicles, not just the mission sides); doGenerate blocks while any deliver objective lacks a delivery point (lib.mjs throws as backstop). Map: checkered-flag badge + dashed line + radius circle in both views.
- **destroy** (4th type, 2026-07-31) = LayerTaskDestroy + SlotDestroy (+ `m_bCanBeGarbageCollected 0`); completion = TaskDestroy.et's `SCR_TaskDestroyObject` damage hook (+ vehicle engine-drowned path). Pool = `DESTROY_OBJECTS` in catalogue.mjs (curated props with shipped thumbnails; provenance `input/destroy-objects.md`) + a Vehicles category the web modal builds from the mission factions' `vehicles` dicts (`web/src/lib/destroyObjects.ts`). **Prop destruction is DISABLED engine-wide (playtest-caught 2026-08-01)**: `DestructionMultiPhase_Base.ct` sets `Enabled 0` on `SCR_DestructionMultiPhaseComponent` and BI re-enables it per prefab (towers yes, antennas/radios/gas tanks/caches/generators no; fuel bowsers even re-disable over an enabled base) — `SCR_DestructibleBuildingEntity` (VOR/silo) and Turret damage (mortars, vehicles) are separate always-on systems. So `objectRef` stays the STORED identity (mission JSON/thumb key, never changes) and lib.mjs resolves what actually spawns: entries with `fix` → mission-local `Prefabs/DestroyTargets/Dest_<name>.et` (parent = the entry's `spawnRef` E_ variant, overrides the multiphase component-instance GUID with `Enabled 1`; radios also get FX config — their vanilla component is empty and would vanish silently); entries with only `spawnRef` → the E_ variant directly; neither (mortars, vehicles) → objectRef as-is. E_ variants everywhere = GM-manipulable during events + replication re-enabled where base props disable it. All GUIDs (resource + inner FX) are FIXED in the catalogue so every generated mission ships byte-identical DestroyTargets files (loadout-conf collide-by-design pattern). Approach radars (2026-08-01, fix-only entries — parent = base ref, no E_ variant or baked preview exists → glyph placeholder): RPL-5 is vanilla-destructible (10000 HP, DamageReduction 150 → explosives/AT only) and its Dest_ prefab replicates the `_on` variant body (rotating antenna + hum; `_on` has no harvestable GUID — binary worlds only) + a fresh SCR_EditableEntityComponent; TPN-19 has NO destruction in its chain → its Dest_ prefab ADDS a fresh ct-inherited multiphase (1500 HP, own-mesh debris) + RplComponent + editable component (PLAYTEST-RISK: only target BI never made destructible). UI: card carries a "Target object" row opening a full-screen modal — category filter chips + scrollable thumbnail grid; thumbs = extracted GM editor previews in `web/public/icons/prefabs/` (200×150, tool: `generator/tools/extract-thumbnails.mjs` + texconv; vehicle thumb name = prefab basename + .png; factions without baked previews — UK, MEI reskins, 4 vanilla oddballs — fall back to a glyph placeholder via `<img>` onError). Default target on placement = `DEFAULT_DESTROY_OBJECT` (Soviet cache; also the migrate() backfill so a destroy objective can never reach lib.mjs without a ref).

## Props (Объекты, 2026-08-04)

Props tab (step 07, after Objectives; markers→08, briefing→09) — place ~96 curated prop prefabs (source list: Obsidian `Object placement.md`) as plain world entities in `Props.layer`, with optional enemy defense groups in `AO.layer`.

- **Registry** = `generator/props.mjs` (`PROPS`/`PROP_CATEGORIES`/`DEFAULT_PROP`, re-exported via catalogue.mjs): 6 categories (militaryBase/fortification/minefield/cargo/wreck/other; `defense: true` on the first two = may carry a defense group; the original Obstacles category was DISSOLVED 2026-08-04 — barricades/checkpoints moved into fortification so they take defense groups, vehicle obstacles/barbed wire/hedgehogs dropped from the pool). Refs are GM-editable E_ variants (GUIDs harvested 2026-08-04 from `Configs/Editor/PlaceableEntities` — the reference dump has no `.meta`); **entries with NO E_ mirror in vanilla** (3 MGN weapon variants, CrateStack_01_dark, Excrement_01, wrecks BRDM2/LAV25/Mi8MT/S1203/UAZ452/UH1H) carry the base prefab + `editable: false` (user-accepted: spawns fine, not GM-editable; UI shows a hint).
- **Footprints** (`fp` per entry, synced by `generator/tools/harvest-prop-footprints.mjs` — don't hand-edit): FULL XZ sizes, `w` = local X, `len` = local Z, `offX/offZ` = box-center offset, rotates with yaw; `{ d }` = disc. Sources: slotted compositions = `PhysicsBoxGeometry UserAction` `Extents` (+ optional `Offset`) walked up the `.et` inheritance chain (Extents is FULL size, not half); plain props/wrecks = `.xob` header AABB (float32-LE min @0x18, max @0x24 — first xob along the chain); minefields = the `SCR_EffectsModuleAreaMeshComponent` sizes (Small 15×15 / Medium 35×35 / Large 75×75 / Rect_Row 5×10 — `m_fWidth/m_fLenght` are full diameters); slot-class fallback (5 FIA supply caches → 15 m disc). **Facing: prefabs are authored front-toward local +Z and yaw is a compass bearing (0 = north, clockwise)** — MG nests/bunkers fire toward +Z, checkpoint/barricade road axis runs along local Z (evidence: MG emplacement + wire at the composition's +Z edge, `RoadNetworkBridgeComponent` points along Z).
- **Emission (lib.mjs)**: `mission.props[]` `{ref, pos, rotation, defense: null|{sizes}}` → `Props.layer` gets `GenericEntity Prop<N> : "<ref>" { coords … angles pitch yaw roll }` (omitted when all 0). **Terrain tilt (2026-08-04)**: with `options.sampleY` present, props align to the slope like Workbench snap-to-terrain — pitch = +atan(slope along local +Z), roll = −atan(slope along local +X), lever arms = footprint half-extents (min 2 m), clamp ±30°; sign convention ground-truthed against terrain-snapped entities in extracted community worlds (Ruha GravelPile, Takistan containers — see propTilt comment). Minefield modules stay flat (logical area). generate.mjs CLI loads `web/public/heightmaps/<terrain>.bin` as its sampler (spikes = terrain-accurate, terrains without a heightmap fall back flat). Defended props append **distinctly-named** `AreaPropDef<N>`/`LayerPropDef<N>`/`SlotAIPropDef<N>` trios to AO.layer: `m_bDynamicDespawn 1` ONLY (despawn range stays at the class default 750, no `m_fAreaRadius`), SlotAI spawns `resolvePropDefenseGroup(enemy, enemyGroupSets, sizes, propIndex)` — merged pool of the 5-stop patrol-weight window (`PATROL_WEIGHT_STOPS`, exported from ZonesPanel and reused by the prop card slider), deterministic round-robin by prop ordinal; the slot's default 30 m defend waypoint does the rest. lib.mjs throws if `defense` reaches a non-defense category.
- **Web**: `MissionProp` in mission.ts (migrate: unknown ref → `DEFAULT_PROP` backfill keeping placement, defense dropped for non-defense categories); `PlaceMode` union type extracted to mission.ts (was written longhand in 4 files); flow = PICK-FIRST (UX feedback 2026-08-04): "Add prop" opens the picker modal → picking a prefab arms `placeMode "prop"` + `pendingPropRef` → one-shot map click places it; the card's picker row reuses the modal to swap in place, and the modal auto-scrolls its grid to the current selection on open. `ObjectThumb`/`ObjectPickerModal` extracted from ObjectivesPanel to `web/src/components/ObjectPicker.tsx` (glyph + categories now params, ObjectivesPanel re-imports). `web/src/lib/props.ts` = propEntry/propThumb/propRect + THUMB_FALLBACKS. Map (both views): green `#4ade80` accent (`PROP_COLOR`/`propBadgeHtml`), draggable badge + true-scale dashed footprint (rect via `itemWorldCorners` with offX/offZ, disc via circle) + **facing tick** (front-edge midpoint → 4 m outward along local +Z), all live-following drags; 3D = draped lines (sector recipe). Uneven-terrain warning per card at **>3 m** delta via `propSlopeDelta` (export.ts, 9-point sample like spawnSlopeDelta).
- **Thumbnails**: 81/104 have baked GM previews (extract-thumbnails basename fallback even found `E_MachineGunNest_*_M2HB/M60/PKM.edds` — BI bakes previews for variants that have no E_ prefab). No preview at all (→ glyph tile): 12 minefield modules, vehicle obstacles, crate stack, S1203 wreck, excrement, SupplyCache_S_FIA_03_Empty (falls back to the non-empty cache thumb); preview-less wrecks map to intact-vehicle thumbs.
- **Playtest gates (pending)**: (a) minefield EffectModules actually arm when baked statically into a layer (GM effect modules — untested outside GM placement); (b) the 13 base-prefab props spawn correctly; (c) defense group spawns at the prop and holds; (d) footprint rectangles match in-game (LivingArea_L 40×40, Helipad 25×25 are the honest tests).

## Loadouts

Full curated character rosters live in `catalogue.mjs` `loadoutSets` per faction → subfaction (source: `input/characters-loadouts.md`; "Don't use" entries excluded; suppressed SF sets reuse parent names; USSR names are Russian by design). The UI "Loadouts" section is a checkbox multiselect over `loadoutSets[playableSubfaction]` (min 1; selection resets on faction/subfaction change; stored as prefab refs in `mission.loadouts`). Selected loadouts go into the `TS_MissionLoadouts.conf` override as `TS_LoadoutConfig` entries with fresh GUIDs (the toolkit default conf is empty — nothing to override in place). The toolkit's conf-based crate (`LoadoutCrates_Conf.et`, `TS_LoadoutSelectorConfComponent`) reads that conf locally on every machine, and the GM "Assign Loadout" action reads it directly — no replication-bubble dependency, no crate loadout override. The crate prefab is still overridden, but only to set `SCR_FactionAffiliationComponent` to the playable faction. `m_iMaxPlayers` is left at default (-1) for now — per-role player caps are a future setting (the conf supports `m_iMaxPlayers`/`m_eLimitScope` per entry when the UI adds them).

## Arsenal

MVP: one baked item set per faction (no UI). Source of truth = `input/arsenal-items.md` (bare `{GUID}path` lines under `## Faction` headings). After editing it, run `node generator/tools/harvest-arsenal.mjs` to re-sync `catalogue.mjs` `FACTIONS[*].arsenalItems` as `{mode, ref}` (modes come from reforger-item-database `data/items.json` `itemMode`, valid `SCR_EArsenalItemMode` tokens, omitted when empty). The generator writes the playable faction's set into the `TS_CustomArsenal.conf` override: first entry reuses the toolkit base conf's member instance GUID `{69092805DE3E6A13}` (Workbench pattern), the rest are fresh-GUID members with `m_iSupplyCost 0`. Clothing is excluded by design (loadouts cover it). **Curation rule (all factions, confirmed 2026-07-29): NO primary weapons (rifles/SMGs/MGs/snipers/pistols) — players spawn with their issued weapon via loadouts. Arsenal = launchers + flare guns/rocket flares, unlimited ammo/magazines, mines/explosives/detonators, medical, utility (radio/binocs/backpacks) — mission-critical kit that pre-defined loadouts might not include.** Optional `FACTIONS[*].subfactionArsenalItems` (keyed like `riflemen`) appends subfaction-specific extras — used for camo-matched backpacks (RHS); UK/vanilla backpacks are faction-wide in `arsenalItems`. Item database tool: `D:\VSCode_dev\arma-reforger\reforger-item-database` (browsable catalog + its own conf generator).

## Vehicles

Full vanilla vehicle catalogue (source: `input/vehicles.md`) in `catalogue.mjs`: `vehicles`/`vehicleLabels` = spawn-picker list per faction (everything incl. armed + helicopters); `patrolVehicleKeys` = mounted-patrol CANDIDATES (armed/armored base variants, keys into `vehicles`, ordered light→heavy). Which candidates actually patrol is a **per-zone multiselect** on the Mounted Patrols module (`ZoneModule.vehicles` keys, min 1, defaults to the lightest; selections reset when the enemy faction changes since keys are faction-specific). Faction assignment: `_FIA` suffix → FIA, Soviet designs → USSR, US designs (incl. MERDC variants, LAV-25, all UH1H) → US.

## Artillery support

Players-tab toggle + per-shell rows (HE/Smoke/Illum, 0–999) in `mission.arty`. Export flattens to `arty: {he, smoke, illum}` round counts (unchecked shell → 0) or `null` when the toggle is off. Generator (`fireSupportBlock` in lib.mjs) writes a `TS_FireSupportManagerComponent "{6995258B6DFFBB1F}"` (= `K.CMP_FIRE_SUPPORT`) override on the GameModeSF entity instance in default.layer: `m_bEnabled 1` + round counts, omitting counts equal to the prefab-effective defaults (HE 60 / Smoke 30 / Illum 30 — set in the toolkit's GameModeSF.et). `m_bEnabled` defaults to 0 in script and the prefab doesn't set it, so toggle-off = no block at all. The component has NO per-type enable flags — 0 rounds removes that round type in-game. `m_iRoundsHeavy` is intentionally not exposed.

## Markers

Markers tab (military + vanilla, mirroring ts-ops-planner's New Marker panel; no TS markers, no phonetic text substitution). Catalogue in `web/src/lib/markers.ts`: military = 48 pre-rendered PNGs (`web/public/icons/military/land-<faction>-<type>.png`, copied from ts-ops-planner), vanilla = atlas sprites (`web/public/icons/vanilla-markers.png`, CSS-mask recolored). `mission.markers` → export maps web keys to game enum tokens → `generator/lib.mjs` emits `Markers.layer` (ONE Area → ONE Layer → ONE `$grp SlotMarker.et` group with named `SlotMarkerN` bodies, Area-relative coords, fields omitted at class defaults; fixed prefab-instance GUIDs in `K.CMP_SF_SLOTMARKER`/`K.MARKER_TYPE_GUID`). **Gotchas (ground-truthed):** `m_eMapMarkerColor` (SCR_EScenarioFrameworkMarkerCustomColor) indexes the game's placed-color array by ORDINAL, so enum names ≠ rendered colors (RED renders dark red, OPFOR red, DARK_PINK dark brown — see MARKER_COLORS in markers.ts); the icon enum has no DIRECTION_OF_ATTACK_MAIN_PLANNED (that ops-planner sprite is excluded); military type is written to BOTH m_eMapMarkerType1Modifier and Type2 (they're OR-ed, both default INFANTRY), omitted for infantry, `0 0` for empty. Marker placement mode stays active for multi-drop; markers are draggable; click selects for live editing.

## Sectors (map-overlay rectangles)

Third Markers sub-tab (Military/Vanilla/Sectors, sub-tab state lifted to page.tsx as `markersTab` so map clicks can force it; RU calls sectors "Районы" — Район операции / Район задачи). No panel list — sectors are managed purely on the map (buttons + edit pane only; tab bar hides while a sector is selected). Corner handles resize with the OPPOSITE corner fixed. AI zones render purple `#9333ea` so red objectives stay distinct. `mission.sectors` = `MissionSector { kind: "ao"|"objective", x, z, length, width, rotation }` — **length spans local X** (ShapePoint X = ±length/2, `itemWorldCorners` `w`), **width spans local Z** (±width/2, `len`); max ONE `ao` (button disables; migration drops extras). Drawn by drag (pointer events, map pan suspended, min 10 m per axis, same path on touch); selectable/movable ONLY via the outline (hit layer = `fill:false` polygon with invisible fat stroke → interior stays click-through); selected sector gets 4 center-anchored corner-resize handles + a rotation handle (`drawSector` in MissionMap.tsx). AO web fill = donut polygon (world-bounds outer ring + rect hole, Leaflet's default evenodd) ≈ in-game `m_FillOutside`. Generator: appended top-level to Markers.layer (after the AreaMarkers block; valid alone) as ONE `$grp PolylineShapeEntity : K.MAPOVERLAY_PREFAB` with anonymous instances — AO = pure prefab defaults (NO component block, black outline/dark outside), objective = fixed red `TS_MapOverlayComponent` override (`K.CMP_MAPOVERLAY`); size via per-instance ShapePoint Position overrides using the prefab's fixed point GUIDs (`K.MAPOVERLAY_POINTS`, never uniform `scale`), rotation = `angles 0 yaw 0` (omitted at 0), point Y always 0. AI-zone display names are "Zone n"/"Зона n" (`zoneName` in i18n.tsx) — generated entity names stay `Area1…`.

## Mission thumbnails

User-uploaded image → one `.edds` driving all three mission-header fields (`m_sIcon`/`m_sLoadingScreen`/`m_sPreviewImage`). **`.edds` is written entirely offline — Workbench converts nothing** (validated end-to-end 2026-07-31: a file written by `web/src/lib/edds.ts` registered, decoded and thumbnailed in Workbench).

- **Format** = standard 124-byte DDS header + ASCII `ENF1` at `0x24` (reserved1[1] — present in all 4377 vanilla + 67 community `.edds` scanned) + standard DX10 header + **Enfusion mip table at `0x94`** (one 8-byte entry per mip: 4-byte tag `COPY` (raw) / `LZ4 ` + 4-byte stored size, ordered **smallest mip first**) + payloads concatenated in table order. The GUID is NOT in the binary — the sidecar `.edds.meta` is the sole source, so self-assigned GUIDs work exactly like `.conf`/`.ent`. Both `.meta` and source images are stripped at pak time (vanilla paks: 28956 `.edds`, 0 `.png`, 0 `.meta`).
- **We emit BC7 mode 6** (dxgi 99 `BC7_UNORM_SRGB` — the most common vanilla format; all mips `COPY`, so no LZ4 needed; floor-halved mip chain matching Workbench level-for-level). 1920×1200 → 2.93 MB, PSNR 47 dB, ~370 ms. Uncompressed BGRA (dxgi 91) also loads fine and is 11.7 MB. `pitchOrLinearSize` = top-mip byte size in both cases.
- **Ship the source `.png` next to the `.edds`.** An orphan `.edds` loads, registers and thumbnails fine, but Workbench's `TextureImportTool` reports `Source file not found` (this is the editor's reimport path, not the loader). Sources cost nothing in the published pak. Write the `.png` BEFORE the `.edds` so the `.edds` isn't treated as stale.
- **One Workbench launch is enough to publish — no restart.** A `RESOURCES (E): Wrong GUID ... {0000000000000000}` line appears only when textures are dropped into a project whose `resourceDatabase.rdb` was already built without them. Generated addons ship no rdb, so their first full scan registers our GUID cleanly (verified by wiping the rdb: clean log, GUID present in the rebuilt rdb).
- **Compositing** (`web/src/lib/thumbnail.ts`; template `web/public/thumbnail-template.png`, 1920×1200): photo cover-fitted UNDER the template, mission name in Roboto Slab Bold **140px on baseline y=1041**, ink-left **x=154**. Position by measured INK, not nominal metrics — cancel the first glyph's side bearing via `actualBoundingBoxLeft`, and do NOT anchor to cap height (lowercase ascenders like `h` sit ~7px above caps, so cap-anchoring lifts any name containing one). A leading "Operation " is stripped since the template already prints it; long names shrink to 70px before overflowing. Uploads are stored as JPEG data URLs (a 1920×1200 PNG would blow the localStorage budget and the `.json` handoff).

## Known gaps / next steps

- RHS content is a STARTER cut pending curation review: loadout rosters (primary variants only, no `_2`/`_3` cosmetic variants), baked arsenal sets (~20 items/faction picked ad-hoc, not yet in `input/arsenal-items.md` / harvest-arsenal.mjs flow), fortification road pools reuse RHS Checkpoint/Barricade editables (placement fit unverified in-game), MSV `ManeuverGroup` bucketed medium (prefab declares only 2 unit slots). USAF FORECON + ION-as-playable need GUIDs from a live Workbench/.meta source.
- Per-loadout player caps (m_iMaxPlayers/m_eLimitScope UI); user-editable arsenal contents (MVP ships baked per-faction sets).
- QRF retrigger (`m_bRetrigger`/cooldown) not exposed — reinforcement waves are one-shot per mission.
- Heightmap-based Y sampling for arbitrary placement (ts-ops-planner `web/src/lib/heightmap.ts` + `tools/map-gen/extract_heightmap.py` assets).
- Web app scaffold + mission JSON schema formalization.

## External references

- Unpacked vanilla data (text `.conf`s, EBIN worlds): `D:\VSCode_dev\arma-reforger\reference\ReforgerData`
- Production mission examples (override patterns, faction setups): `C:\Users\djdav\Documents\My Games\ArmaReforgerWorkbench\addons\Operation *`
- enfusion-mcp offline generator precedent: `C:\Users\djdav\mcp-servers\enfusion-mcp-BK\src\templates\scenario.ts`
